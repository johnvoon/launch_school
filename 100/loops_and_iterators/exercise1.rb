x = [1, 2, 3, 4, 5] #stores an array of 5 elements in the variable x
x.each do |a| #iterates the x array and for each element stores it in the variable |a|
  a + 1 #for each element, add one to it; do the same for each element in the array
end
